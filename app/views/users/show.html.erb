<%= render partial: "shared/user", locals: {user: @user} %>
<div>
<h3>Bio</h3>
<%= @user.bio  %>
</div>

<% @posts.each do |post| %>
    <%= link_to user_post_path(@user.id), class: 'link text-dark' do %>
       <%= render partial: "shared/post", locals: {post: post} %> 
    <%end%>
<%end%>

